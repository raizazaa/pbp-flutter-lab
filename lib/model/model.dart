import 'package:flutter/material.dart';

class Model {
    String title;
    int amount;
    String date;
    String type;

    Model( {
        required this.title,
        required this.amount,
        required this.date,
        required this.type,
    });
}